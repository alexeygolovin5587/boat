# Automatically created by: shub deploy

from setuptools import setup, find_packages

setup(
    name         = 'boat',
    version      = '1.0',
    packages     = find_packages(),
	 scripts      = ['bin/boat_scrapy.py'],
    entry_points = {'scrapy': ['settings = boat.settings']},
)
